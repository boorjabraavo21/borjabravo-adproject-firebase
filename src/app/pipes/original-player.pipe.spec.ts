import { OriginalPlayerPipe } from './original-player.pipe';

describe('OriginalPlayerPipe', () => {
  it('create an instance', () => {
    const pipe = new OriginalPlayerPipe();
    expect(pipe).toBeTruthy();
  });
});
